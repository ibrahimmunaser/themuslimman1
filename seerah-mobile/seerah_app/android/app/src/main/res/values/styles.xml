<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding.

         Audit H11 fix: the app's Flutter UI (main.dart's MaterialApp.router)
         only ever uses AppTheme.dark — there is no light theme, regardless
         of the OS-level Dark Mode setting this values/ (non-night) variant
         is selected for. Theme.Light.NoTitleBar's colorBackground resolves
         to white, so on a device with system Dark Mode OFF, the gap between
         the (black) launch splash being dismissed and Flutter's first frame
         painting showed a white flash. Theme.Black.NoTitleBar's
         colorBackground is black, matching values-night/styles.xml and this
         app's actual, permanently-dark UI. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
