security: comprehensive QA cycle — fix critical/high/medium issues

Critical:
- webhook: handleGiftPaymentSuccess throws on missing metadata (no longer silently drops gift fulfillment)
- webhook: handleSubscriptionDeleted throws on unresolved userId (prevents planType stuck as family)
- webhook: syncSubscriptionStatus upserts when updateMany affects 0 rows (fixes event ordering race)
- quiz: strip correct_answer from RSC payload at all 3 server boundaries before client serialization

High/Medium:
- progress: quizBestScore update now atomic via conditional updateMany (no more race condition)
- resend-verification: fullName null fallback prevents broken greeting
- dev/grant-access: DELETE handler now requires ALLOW_DEBUG_ENDPOINTS (matches POST guard)

Lint/build health:
- Replace <a href="/seerah"> with <Link> in 3 components (ESLint errors)
- Fix let to const, unused imports, unused eslint-disable directives across 8 files
- TypeScript: 0 errors, ESLint: 0 errors/warnings, production build: pass

Also includes all 35 fixes from previous QA cycle:
- hashToken extracted to shared lib/hash-token.ts
- quiz API strips correct_answer; server action validates answers
- timing-safe login (dummy bcrypt on unknown user)
- httpOnly on seerah_role cookie
- rate limiting on reset-password and mobile-purchases
- atomic trackAssetOpened via raw SQL JSONB update
- webhook throws instead of silent return for missing userId
- free-access deterministic ID + idempotent transaction
- gift claim removes recipientEmail from 403 response
- allow_promotion_codes: true on checkout sessions
- payment_intent.payment_failed updates purchase status in DB
- cron replay protection via X-Cron-Timestamp header
- password max(1000) in signup and reset
- debug endpoints hardened with ALLOW_DEBUG_ENDPOINTS env var
